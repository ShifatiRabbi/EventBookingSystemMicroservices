🔥 STEP 1: Stop everything
docker compose down

🔥 STEP 2: Remove containers, networks, volumes (FULL WIPE)

⚠️ This deletes all databases.

docker compose down -v --remove-orphans

🔥 STEP 3: Remove dangling images & cache (optional but clean)
docker system prune -a


If asked → type y

🔥 STEP 4: (Optional) Remove Minikube completely

Only if you installed it:

minikube stop
minikube delete


You can uninstall later if you want — not required.

4️⃣ Correct way to rebuild & start the system (IMPORTANT)

From project root (where docker-compose.yml exists):

✅ STEP 1: Build images (after code changes)

Run this when you change application code:

docker compose build


If only one service changed:

docker compose build booking-service

✅ STEP 2: Start everything
docker compose up -d

✅ STEP 3: Verify all containers are running
docker ps

7️⃣ When to run WHICH command (cheat sheet)
🧠 Code change (JS/TS only)
docker compose build <service>
docker compose up -d

🧠 Dockerfile change
docker compose build
docker compose up -d

🧠 docker-compose.yml change
docker compose down
docker compose up -d

🧠 SQL schema change
docker compose down -v
docker compose up -d
